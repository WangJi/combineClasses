{
  "name": "partial_classes",
  "scripts": {
    "build:cjs": "tsc --outDir dist/cjs --module commonjs --declaration false",
    "build:esm": "tsc --outDir dist/esm --module esnext  --declaration true --declarationDir dist/types",
    "build": "npm run build:cjs && npm run build:esm",
    "test": "npx ts-node text.ts"
  },
  "version": "1.0.3",
  "description": "A ts utility provides function to combine ts classes with strong type support",
  "main": "dist/cjs/index.js",
  "module": "dist/esm/index.js",
  "types": "dist/types/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}